import _extends from "@babel/runtime/helpers/extends";
import React from 'react';
import PropTypes from 'prop-types';
import Icon28ChevronBack from '@vkontakte/icons/dist/28/chevron_back';
import Icon24Back from '@vkontakte/icons/dist/24/back';
import HeaderButton from '../HeaderButton/HeaderButton';
import { IS_PLATFORM_ANDROID } from '../../lib/platform';
var PanelHeaderBack = React.memo(function (_ref) {
  var restProps = _extends({}, _ref);

  return React.createElement(HeaderButton, restProps, IS_PLATFORM_ANDROID ? React.createElement(Icon24Back, null) : React.createElement(Icon28ChevronBack, null));
});
PanelHeaderBack.propTypes = {
  onClick: PropTypes.func.isRequired
};
export default PanelHeaderBack;
//# sourceMappingURL=PanelHeaderBack.js.map